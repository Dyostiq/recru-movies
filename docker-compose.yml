version: "3.2"
services:
  auth:
    command: node src/server.js
    build:
      context: auth
      dockerfile: Dockerfile
    ports:
      - published: ${APP_PORT:-3000}
        target: 3000
    volumes:
      - ./auth:/app/
    environment:
      JWT_SECRET: ${JWT_SECRET}
